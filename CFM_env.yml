# This file may be used to create an environment using:
# $ conda create --name <env> --file <this file>
name: CFMenvironment
channels:
  - defaults
  - conda-forge
dependencies:
  - tensorflow=2.9.1
  - python=3.8
  - numpy
  - scipy
  - matplotlib
  - pandas
  - scikit-learn
  - tqdm
  - obspy
  - uvicorn
  - fastapi
  - keras